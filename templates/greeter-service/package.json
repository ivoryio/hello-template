{
  "name": "greeter-service",
  "version": "0.0.1",
  "description": "Greets anyone making an API call, also offers the capability to add new greetings.",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p build && aws cloudformation package --template-file infrastructure/template.yaml --output-template-file build/infrastructure.packaged.yaml",
    "deploy": "aws cloudformation deploy --template-file build/infrastructure.packaged.yaml --capabilities CAPABILITY_NAMED_IAM"
  }
}
